# Runtime dependencies for the Gamira backend.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
SQLAlchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
aiosqlite==0.20.0
greenlet==3.1.1
python-json-logger==3.2.1
httpx==0.28.1
PyJWT[crypto]==2.10.1
python-dateutil==2.9.0.post0
tzdata==2024.2

# AI and notifications. Both are imported lazily, so the API and the worker
# start and serve every non-AI path without them — but AI_PROVIDER=gemini and
# FCM_PROVIDER=firebase need them present.
google-genai==2.18.1
google-auth==2.56.3
