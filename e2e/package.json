{
  "name": "gamira-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end suite driving the real Gamira backend and both real frontend dev servers together. Standalone package, not part of any workspace.",
  "type": "module",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1"
  }
}
